/**
 * Génération d'indexes optionnels.
 *
 * Objectif:
 * - Accélérer la recherche et les accès (name tokens, places, xrefs)
 * - Doit rester optionnel: reconstructible
 *
 * Fonctions à implémenter:
 * - buildIndexes(bundle: CanonicalBundle): BundleIndexes
 */
export function buildIndexes(_bundle: unknown): unknown {
  // TODO: impl
  return {};
}
