/**
 * Diff entre 2 CanonicalBundle.
 *
 * Objectif:
 * - Détecter ajout/suppression/modification d'entités
 * - Sortie compacte pour historiser ou afficher un changelog
 *
 * Fonctions à implémenter:
 * - diffBundles(a: CanonicalBundle, b: CanonicalBundle): DiffResult
 * - diffEntityLists(...)
 */
export function diffBundles(_a: unknown, _b: unknown): unknown {
  // TODO: impl
  return { added: [], removed: [], updated: [] };
}
