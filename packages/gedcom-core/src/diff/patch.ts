/**
 * Patch (optionnel) : appliquer un DiffResult sur un bundle.
 *
 * Objectif:
 * - Rejouer des changements
 * - Utile si tu stockes des diffs plutôt que des bundles complets
 *
 * Fonctions à implémenter:
 * - applyPatch(bundle: CanonicalBundle, diff: DiffResult): CanonicalBundle
 */
export function applyPatch(_bundle: unknown, _diff: unknown): unknown {
  // TODO: impl
  return _bundle;
}
