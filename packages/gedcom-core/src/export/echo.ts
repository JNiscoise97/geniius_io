/**
 * Export Echo.
 *
 * Objectif:
 * - Convertir CanonicalBundle -> payload optimisé pour Echo
 * - Echo peut consommer un sous-ensemble (métadonnées, profils, tags)
 *
 * Fonctions à implémenter:
 * - exportEcho(bundle: CanonicalBundle, opts?): EchoPayload
 */
export function exportEcho(_bundle: unknown): unknown {
  // TODO: impl
  return {};
}
