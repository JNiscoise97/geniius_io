/**
 * Export JSON générique.
 *
 * Objectif:
 * - Sortie stable (debug, intégrations, tests)
 * - Peut être identique au CanonicalBundle ou une version "sans indexes"
 *
 * Fonctions à implémenter:
 * - exportJson(bundle: CanonicalBundle, opts?): any
 */
export function exportJson(_bundle: unknown): unknown {
  // TODO: impl
  return _bundle;
}
