/**
 * Export Rebond.
 *
 * Objectif:
 * - Convertir CanonicalBundle -> payload optimisé pour Rebond
 * - Gérer mapping de champs, IDs, et éventuellement indexes spécifiques
 *
 * Fonctions à implémenter:
 * - exportRebond(bundle: CanonicalBundle, opts?): RebondPayload
 */
export function exportRebond(_bundle: unknown): unknown {
  // TODO: impl
  return {};
}
