/**
 * Builder média canonique.
 *
 * Objectif:
 * - Extraire OBJE/FILE/FORM/TITL etc.
 * - Ne JAMAIS embarquer le binaire: seulement références (uri/path/mime)
 *
 * Fonctions à implémenter:
 * - buildMediaEntities(parsed): Media[]
 * - linkMediaToTargets(media, targetIds)
 */
export {};
