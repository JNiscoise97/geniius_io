/**
 * Normalisation des dates GEDCOM (DATE).
 *
 * Objectif:
 * - Parser ABT/BET/BEF/AFT/EST, périodes, années seules, textes
 * - Produire une CanonicalDate structurée
 *
 * Fonctions à implémenter:
 * - parseGedcomDate(raw: string): CanonicalDate
 * - toIsoIfPossible(...)
 * - parseMonth(...)
 */
export {};
