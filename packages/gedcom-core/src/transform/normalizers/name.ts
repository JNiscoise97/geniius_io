/**
 * Normalisation des noms (NAME).
 *
 * Objectif:
 * - Harmoniser casse, accents, particules, espaces multiples
 * - Gérer variantes, alias, noms de naissance/mariage
 *
 * Fonctions à implémenter:
 * - normalizeFullName(raw: string): { full, normalized, given?, surname? }
 * - normalizeSurname(s: string): string
 * - normalizeGiven(g: string): string
 * - tokenizeNameForSearch(name: string): string[]
 */
export {};
