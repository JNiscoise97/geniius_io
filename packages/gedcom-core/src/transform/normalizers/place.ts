/**
 * Normalisation des lieux (PLAC).
 *
 * Objectif:
 * - Nettoyer et découper les lieux "Commune, Département, Pays"
 * - Harmoniser séparateurs, espaces, variantes
 *
 * Fonctions à implémenter:
 * - normalizePlace(raw: string): CanonicalPlace
 * - splitPlace(raw: string): string[]
 * - tokenizePlaceForSearch(place: CanonicalPlace): string[]
 */
export {};
